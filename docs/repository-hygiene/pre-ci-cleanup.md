# Pre-CI Repository Hygiene

## Scope

This cleanup gate runs after `PACKAGE-005` and before `CI-001`. It keeps local
build products, dependency installs, and packaged artifacts out of Git while
preserving user files and caches on disk.

## Inventory

- `apps/desktop/node_modules/`: local Bun dependency install; now ignored.
- `apps/desktop/dist/`: frontend production output generated by desktop and
  package builds; now ignored.
- `apps/desktop/src-tauri/target/`: Rust and Tauri build output; now ignored.
- `apps/desktop/src-tauri/gen/`: generated Tauri schema/output; now ignored.
- `tools/package/windows/artifacts/`, `build/`, and `reports/`: already ignored
  by `tools/package/windows/.gitignore`.
- `apps/desktop/src-tauri/resources/runtime*` and `staging-reports/`: already
  ignored by `apps/desktop/src-tauri/resources/.gitignore`.

## Tracked Artifact Audit

No tracked `node_modules`, desktop `dist`, Tauri `target` or `gen`, package
ZIP, package build, package report, or package artifact output was found during
the cleanup audit.

The unrelated local `docs/PRD.md` file remains untracked and untouched.
