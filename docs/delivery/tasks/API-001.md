# API-001 — API CLI, Configuration, Handshake, and Contracts

**Implementer inference:** Low  
**Prerequisites:** `FEAS-003` approved with `GO`.

## Frozen inputs

Use `DEPENDENCY-PINS.md`, `CONTRACTS.md`, `TIMEOUTS.md`, and `PACKAGING.md` exactly. Do not select package versions, CLI options, origins, schemas, errors, exit codes, manifest fields, timeouts, or artifact versioning.

## Objective

Create the production Zig API shell, the shared cross-process contracts, and buildable static seams that prevent later packets from editing shared build or entrypoint files.

## Procedure

1. Create root `VERSION` with exactly `0.1.0` and a trailing newline.
2. Pin all approved Zig/native dependencies from `DEPENDENCY-PINS.md` and establish reproducible builds.
3. Implement `serve`, `self-test`, and `version` with the exact CLI-only configuration, mode validation, origins, external flags, listen exception, handshake, tokens, and mandatory exit codes from `CONTRACTS.md`.
4. Bind and emit the validated handshake before asynchronous database initialization; send structured logs only to stderr and apply `TIMEOUTS.md`.
5. Check in exact status, error, handshake, runtime-manifest, writable-manifest, and desktop-snapshot schemas plus valid and invalid fixtures.
6. Create buildable stubs and static imports for every later PostgreSQL, TigerBeetle, HTTP, status, runtime-platform, runtime-manifest, runtime-logging, managed-component, and supervisor module.
7. Create one static aggregate test registration that imports every stub so later tasks replace only files in their owned subdirectories.
8. Pin the full future direct dependency set required by approved API packets now. Later tasks may not edit `build.zig`, dependency manifests, `main.zig`, `VERSION`, or aggregate test registration.
9. Prove no environment variable or implicit configuration source affects behavior.
10. For pg.zig, verify the exact upstream base, apply only the recorded
    reviewer-approved patch deterministically, and reject a missing or
    mismatched patch SHA-256.
11. Include exit code `7` and `native_shutdown_timeout` in the shared schemas
    and fixtures.

If any later module cannot be implemented through the predeclared seam, stop and request coordinator reassignment; do not leave a future worker to edit a shared build or entrypoint file.

## Acceptance evidence

- `zig version` reports `0.15.2`.
- `zig build` and `zig build test` succeed from a clean cache and exercise every static stub import.
- Golden handshake and malformed-configuration tests.
- Token uniqueness and redaction tests.
- Contract fixture validation from Zig and TypeScript test consumers, including all exact status/error shapes and external/managed flag matrices.
- A manifest/lock/pin audit against `DEPENDENCY-PINS.md`, plus `git diff --check`.
- Exact-base pg.zig patch/hash rejection tests and Windows-only current-target
  contract checks.

## Reviewer focus

Verify exact CLI and wire formats, stdout purity, CSPRNG use, validation order, and absence of secrets from all error paths.
