# DOC-001 — Create ADRs and Repository Governance

**Implementer inference:** Low  
**Prerequisites:** `RESET-001` approved.

## Frozen inputs

Translate `ARCHITECTURE.md`, `DECISIONS.md`, `DEPENDENCY-PINS.md`, `CONTRACTS.md`, `TIMEOUTS.md`, and `PACKAGING.md` without changing their substance.

## Objective

Turn the frozen planning decisions into durable ADRs and repository guidance without changing their substance.

## Procedure

1. Create ADRs for local-first process boundaries; PostgreSQL/TigerBeetle ownership; Zig/native clients; managed/external modes; authentication and handshake; roots and locks; pins/provenance; portable distribution; and static UI module registration.
2. Each ADR records context, decision, rejected alternatives, consequences, and supersession rules.
3. Add a concise privacy statement: local data, no telemetry, loopback traffic, and diagnostic-redaction policy.
4. Update root `AGENTS.md` with worktree, ownership, low-inference implementer, independent review, and verification requirements.
5. Link the v1 retrospective and deferred finance record rather than duplicating them.

## Acceptance evidence

- ADR index and link check.
- Mapping from every frozen architecture decision to an ADR.
- Search proving deferred finance behavior is not described as implemented.
- Markdown link-check command, frozen-decision mapping output, terminology search, and `git diff --check`.

## Reviewer focus

Check every ADR against the frozen plan and reject accidental redesign, vague security boundaries, or claims beyond the first slice.
